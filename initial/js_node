
export function genArrZero(num: number) {
    // your code here
    return undefined;
}

import * as assert from 'assert';

console.log('Example:');
console.log(genArrZero(0));

// These "asserts" are used for self-checking and not for an auto-testing
assert.deepEqual(genArrZero(0), []);
assert.deepEqual(genArrZero(1), [0]);
assert.deepEqual(genArrZero(5), [0, 0, 0, 0, 0]);

console.log("Coding complete? Click 'Check' to earn cool rewards!");
    